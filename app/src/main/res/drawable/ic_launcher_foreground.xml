<?xml version="1.0" encoding="utf-8"?>
<!-- The Sileo wordmark, centered in the adaptive-icon safe zone. The PNG's own
     dark background matches @color/ic_launcher_background, so it reads as one icon. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:width="74dp"
        android:height="32dp"
        android:gravity="center"
        android:drawable="@drawable/sileo_splash" />
</layer-list>
